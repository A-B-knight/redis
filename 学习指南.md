# Redis 学习指南

## 目录
1. [快速开始](#快速开始)
2. [编译和运行 Redis](#编译和运行-redis)
3. [使用 redis-cli](#使用-redis-cli)
4. [Redis 数据类型](#redis-数据类型)
5. [Python 客户端示例](#python-客户端示例)
6. [常见使用场景](#常见使用场景)
7. [学习资源](#学习资源)

## 快速开始

### 方法 1: 使用 Docker（推荐，最简单）

```bash
docker run -d -p 6379:6379 --name redis redis:latest
```

### 方法 2: 从源码编译（你当前的情况）

你已经有了 Redis 源代码，可以按照下面的步骤编译和运行。

## 编译和运行 Redis

### 1. 编译 Redis

在 WSL2 (Ubuntu) 环境下，执行以下命令：

```bash
cd /home/knight/code/redis
make
```

如果需要编译包含所有模块的完整版本（包括 JSON、时间序列等）：

```bash
export BUILD_WITH_MODULES=yes BUILD_TLS=yes
make -j "$(nproc)" all
```

### 2. 运行 Redis 服务器

```bash
# 使用默认配置运行
./src/redis-server

# 或使用配置文件运行
./src/redis-server redis.conf
```

### 3. 测试 Redis 是否正常运行

在另一个终端窗口中：

```bash
cd /home/knight/code/redis
./src/redis-cli ping
```

如果返回 `PONG`，说明 Redis 运行正常！

## 使用 redis-cli

`redis-cli` 是 Redis 的命令行工具，是学习和测试 Redis 的最佳方式。

### 启动 redis-cli

```bash
cd /home/knight/code/redis
./src/redis-cli
```

### 基本命令示例

```redis
# 测试连接
PING
# 返回: PONG

# 设置键值对
SET mykey "Hello Redis"
# 返回: OK

# 获取值
GET mykey
# 返回: "Hello Redis"

# 检查键是否存在
EXISTS mykey
# 返回: (integer) 1

# 删除键
DEL mykey

# 设置过期时间（10秒后过期）
SET mykey "Hello" EX 10

# 查看剩余过期时间
TTL mykey

# 计数器
INCR counter
INCR counter
GET counter
# 返回: "2"

# 列表操作
LPUSH mylist "world"
LPUSH mylist "hello"
LRANGE mylist 0 -1
# 返回: 1) "hello" 2) "world"

# 哈希操作
HSET user:1 name "张三" age 30
HGET user:1 name
# 返回: "张三"
HGETALL user:1
# 返回: 1) "name" 2) "张三" 3) "age" 4) "30"

# 集合操作
SADD myset "apple" "banana" "orange"
SMEMBERS myset
# 返回: 1) "apple" 2) "banana" 3) "orange"

# 有序集合（排行榜）
ZADD leaderboard 100 "player1"
ZADD leaderboard 200 "player2"
ZADD leaderboard 150 "player3"
ZREVRANGE leaderboard 0 -1 WITHSCORES
# 返回排行榜（按分数降序）

# 退出
EXIT
```

## Redis 数据类型

Redis 支持多种数据类型，每种都有其特定的使用场景：

### 1. String（字符串）
- **用途**: 缓存、计数器、简单的键值存储
- **常用命令**: SET, GET, INCR, DECR, APPEND

### 2. Hash（哈希）
- **用途**: 存储对象（如用户信息）
- **常用命令**: HSET, HGET, HGETALL, HDEL

### 3. List（列表）
- **用途**: 队列、栈、消息列表
- **常用命令**: LPUSH, RPUSH, LPOP, RPOP, LRANGE

### 4. Set（集合）
- **用途**: 去重、集合运算（交集、并集、差集）
- **常用命令**: SADD, SMEMBERS, SINTER, SUNION

### 5. Sorted Set（有序集合）
- **用途**: 排行榜、范围查询
- **常用命令**: ZADD, ZRANGE, ZREVRANGE, ZRANK

### 6. Stream（流）
- **用途**: 消息队列、事件日志
- **常用命令**: XADD, XREAD, XGROUP

## Python 客户端示例

### 安装 Python Redis 客户端

```bash
pip install redis
```

### 示例代码

创建一个 `redis_examples.py` 文件：

```python
import redis

# 连接到 Redis
r = redis.Redis(host='localhost', port=6379, db=0, decode_responses=True)

# 测试连接
print(r.ping())  # 输出: True

# 1. 字符串操作
r.set('name', 'Redis学习')
print(r.get('name'))  # 输出: Redis学习

# 2. 计数器
r.set('counter', 0)
r.incr('counter')
r.incr('counter')
print(r.get('counter'))  # 输出: 2

# 3. 哈希操作（存储用户信息）
r.hset('user:1001', mapping={
    'name': '张三',
    'age': 25,
    'email': 'zhangsan@example.com'
})
print(r.hgetall('user:1001'))

# 4. 列表操作（消息队列）
r.lpush('messages', '消息1', '消息2', '消息3')
print(r.lrange('messages', 0, -1))

# 5. 集合操作（标签系统）
r.sadd('article:1:tags', 'Python', 'Redis', '数据库')
r.sadd('article:2:tags', 'Python', 'Web开发')
print(r.sinter('article:1:tags', 'article:2:tags'))  # 交集

# 6. 有序集合（排行榜）
r.zadd('leaderboard', {'player1': 100, 'player2': 200, 'player3': 150})
print(r.zrevrange('leaderboard', 0, -1, withscores=True))

# 7. 过期时间
r.setex('session:abc123', 3600, '用户会话数据')  # 1小时后过期
print(r.ttl('session:abc123'))  # 查看剩余时间

# 8. 批量操作（管道）
pipe = r.pipeline()
pipe.set('key1', 'value1')
pipe.set('key2', 'value2')
pipe.incr('counter')
pipe.execute()
```

## 常见使用场景

### 1. 缓存
```python
def get_user(user_id):
    cache_key = f'user:{user_id}'
    # 先查缓存
    user = r.get(cache_key)
    if user:
        return json.loads(user)
    
    # 缓存未命中，查数据库
    user = db.get_user(user_id)
    # 存入缓存，设置过期时间
    r.setex(cache_key, 3600, json.dumps(user))
    return user
```

### 2. 会话存储
```python
# 存储会话
session_id = 'abc123'
r.setex(f'session:{session_id}', 1800, json.dumps({
    'user_id': 1001,
    'username': 'zhangsan'
}))

# 获取会话
session = r.get(f'session:{session_id}')
```

### 3. 分布式锁
```python
import time

def acquire_lock(lock_name, timeout=10):
    identifier = str(time.time())
    end = time.time() + timeout
    while time.time() < end:
        if r.set(f'lock:{lock_name}', identifier, nx=True, ex=timeout):
            return identifier
        time.sleep(0.001)
    return False

def release_lock(lock_name, identifier):
    pipe = r.pipeline(True)
    while True:
        try:
            pipe.watch(f'lock:{lock_name}')
            if pipe.get(f'lock:{lock_name}') == identifier:
                pipe.multi()
                pipe.delete(f'lock:{lock_name}')
                pipe.execute()
                return True
            pipe.unwatch()
            break
        except redis.WatchError:
            pass
    return False
```

### 4. 消息队列
```python
# 生产者
r.lpush('task_queue', json.dumps({'task_id': 1, 'data': '处理数据'}))

# 消费者
while True:
    task = r.brpop('task_queue', timeout=1)
    if task:
        task_data = json.loads(task[1])
        # 处理任务
        process_task(task_data)
```

### 5. 排行榜
```python
# 添加分数
r.zadd('game:leaderboard', {'player1': 1000, 'player2': 2000})

# 更新分数
r.zincrby('game:leaderboard', 100, 'player1')

# 获取前10名
top_10 = r.zrevrange('game:leaderboard', 0, 9, withscores=True)

# 获取玩家排名
rank = r.zrevrank('game:leaderboard', 'player1')
```

### 6. 限流（Rate Limiting）
```python
def is_rate_limited(user_id, limit=10, window=60):
    key = f'rate_limit:{user_id}'
    current = r.incr(key)
    if current == 1:
        r.expire(key, window)
    return current > limit
```

## 学习资源

### 官方文档
- **Redis 官方文档**: https://redis.io/docs/
- **Redis 命令参考**: https://redis.io/commands/
- **Redis 数据类型**: https://redis.io/docs/latest/develop/data-types/

### 客户端库
- **Python**: https://redis.io/docs/latest/develop/clients/redis-py/
- **Node.js**: https://redis.io/docs/latest/develop/clients/nodejs/
- **Go**: https://redis.io/docs/latest/develop/clients/go/
- **Java**: https://redis.io/docs/latest/develop/clients/jedis/

### 工具
- **Redis Insight**: 可视化管理工具
  - 下载: https://redis.io/docs/latest/develop/tools/insight/
- **redis-cli**: 命令行工具（已包含在源码中）

### 实践建议

1. **从基础开始**: 先掌握 String、Hash、List、Set、Sorted Set 这五种基本数据类型
2. **动手实践**: 使用 `redis-cli` 亲自输入命令，观察结果
3. **编写代码**: 用你熟悉的编程语言编写 Redis 客户端程序
4. **解决实际问题**: 尝试用 Redis 解决实际项目中的问题（如缓存、会话存储）
5. **阅读源码**: 既然你有源码，可以深入理解 Redis 的实现原理

## 下一步

1. ✅ 编译并运行 Redis 服务器
2. ✅ 使用 `redis-cli` 练习基本命令
3. ✅ 安装 Python Redis 客户端并运行示例代码
4. ✅ 尝试实现一个实际项目（如简单的缓存系统）
5. ✅ 深入学习 Redis 的高级特性（事务、Lua 脚本、发布订阅等）

祝你学习愉快！🚀

